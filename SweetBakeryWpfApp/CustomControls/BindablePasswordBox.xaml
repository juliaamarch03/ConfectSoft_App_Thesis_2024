<UserControl x:Class="SweetBakeryWpfApp.CustomControls.BindablePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SweetBakeryWpfApp.CustomControls"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">

    <PasswordBox x:Name="txtPassword"
                 Style="{StaticResource loginViewPasswordtextBoxes}"
                 Padding="20,0,0,0"
                 MaxLength="8">
        <PasswordBox.Background>
            <VisualBrush Stretch="None" AlignmentX="Left">
                <VisualBrush.Visual>
                    <fa:IconBlock Icon="Lock" Foreground="White" FontSize="16" Margin="0,0,10,0"/>
                </VisualBrush.Visual>
            </VisualBrush>
        </PasswordBox.Background>

    </PasswordBox>

</UserControl>
